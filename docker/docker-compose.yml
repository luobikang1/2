version: "3.8"

services:
  baihu:
    build: .
    container_name: baihu
    restart: unless-stopped
    env_file: .env
    ports:
      - "443:443/udp"   # TUIC 直连 (QUIC/UDP)
    environment:
      - TZ=Asia/Shanghai
